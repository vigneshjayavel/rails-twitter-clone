<ul>
	<li>
		<%= image_tag gravatar_image(gravatar_for(user)), :size=>30 %>
		<%= link_to h(user.name), user %>
		<% if current_user.admin? %>
		| <%= link_to("delete", user, :method => :delete, :confirm => "You sure?") unless user.admin? %> 
		<% end %>
	</li>
</ul>